$kind: http-request
name: POST /auth/login
method: POST
url: "{{baseURL}}/auth/login"
headers:
  Content-Type: application/json
body:
  type: json
  content: >-
    {
      "email": "{{jiraEmail}}",
      "jiraApiToken": "{{jiraApiToken}}"
    }
order: 3556376594874367
