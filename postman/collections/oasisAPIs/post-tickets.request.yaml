$kind: http-request
name: POST /tickets
method: POST
url: "{{baseURL}}/tickets"
headers:
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
body:
  type: json
  content: |-
    {
      "projectKey": "KAN",
      "title": "Newer finding after first",
      "description": "Newer finding description",
      "issueType": "Task"
    }
auth:
  type: bearer
order: 4050722423435995
